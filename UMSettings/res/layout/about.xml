<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/trans"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/tvTitle"
        style="@style/title"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:text="@string/about_device" />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="1dp"
        android:orientation="vertical" >

    <ListView 
        android:id="@+id/lvAboutWindow"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical"
        android:scrollbarFadeDuration="0"
        android:fadeScrollbars="false"
        android:nextFocusUp="@id/tvTitle"
        android:listSelector="@drawable/tui_numberpicker_trans"
        android:divider="@drawable/background_divider"/>
      
      </LinearLayout>
    
    <Button 
        android:id="@+id/bt_bottom_device_focus"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:background="@android:color/transparent"/>
    
</LinearLayout>